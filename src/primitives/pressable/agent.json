{
  "schema": "flowstack.agent-component.v1",
  "id": "pressable",
  "name": "Pressable",
  "package": "@flowstack-ui/atom",
  "layer": "atom",
  "kind": "component",
  "purpose": "Add normalized press interaction to a deliberately chosen semantic element without deciding that element's visual design.",
  "useWhen": ["A semantic surface needs unified keyboard, pointer, and press handling and neither Button nor Link alone expresses the composition."],
  "avoidWhen": [{ "condition": "A normal action or destination needs no custom semantic surface.", "useInstead": "Button or Link" }],
  "composition": ["Choose the correct rendered semantic element first, then attach Pressable behavior without nesting interactive controls."],
  "rules": [
    { "id": "pressable-semantics-first", "level": "must", "statement": "Pressable does not supply missing semantics; the rendered element must already express the interaction." },
    { "id": "pressable-no-nesting", "level": "must", "statement": "Do not create nested interactive elements." }
  ],
  "commonMistakes": [{ "mistake": "Using Pressable as a generic clickable div.", "correction": "Prefer Button or Link; use Pressable only for a justified semantic composition." }],
  "validation": ["Verify the rendered role and accessible name.", "Test keyboard, pointer, touch, focus, and disabled behavior."],
  "related": ["button", "link"]
}
