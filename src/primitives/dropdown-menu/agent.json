{
  "schema": "flowstack.agent-component.v1",
  "id": "dropdown-menu",
  "name": "DropdownMenu",
  "package": "@flowstack-ui/atom",
  "layer": "atom",
  "kind": "component",
  "purpose": "Open a compact command or settings menu from a visible button with menu popup semantics, input-aware initial focus, and the shared Menu contract.",
  "useWhen": ["A visible button should reveal a short list of commands, independent settings, exclusive settings, or nested commands."],
  "avoidWhen": [
    { "condition": "Commands belong to a contextual gesture, several persistent application menu headings, a form value, or ordinary site navigation.", "useInstead": "ContextMenu, Menubar, Select, or NavList" }
  ],
  "composition": [
    "Compose Root with a visibly named Trigger and Content. Add Portal and Arrow only when needed; fill Content with uniquely valued Item parts and the shared Menu Group, Label, CheckboxItem, RadioGroup with RadioItem, Separator, or paired Sub anatomy as the command model requires."
  ],
  "rules": [
    { "id": "dropdown-visible-trigger", "level": "must", "statement": "Use a visible, accessible Trigger that describes the menu; preserve its button, popup, expanded, controls, disabled, and custom-element keyboard semantics." },
    { "id": "dropdown-input-focus", "level": "must", "statement": "Preserve input-aware opening: pointer or ordinary activation opens through the owned policy, ArrowDown and activation keys enter at the first item, and ArrowUp enters at the last item." },
    { "id": "dropdown-menu-contract", "level": "must", "statement": "Follow Menu's item roles, unique values, real focus, typeahead, disabled navigation, closeOnSelect, submenu, outside-dismissal, modal, portal, and focus-restoration contracts." },
    { "id": "dropdown-composition-merge", "level": "must", "statement": "When Trigger or retained Menu parts use asChild or render, merge Atom props, refs, handlers, ARIA, and children instead of replacing them." }
  ],
  "commonMistakes": [
    { "mistake": "Using an unlabeled icon trigger, putting ordinary navigation or a form select into DropdownMenu, or adding custom key and document dismissal handlers.", "correction": "Give Trigger a complete accessible name, select the correct semantic component, and rely on the shared Menu behavior." }
  ],
  "validation": [
    "Verify Trigger naming and semantics, click, tap, Enter, Space, ArrowDown, and ArrowUp opening, initial item focus, controlled state, disabled behavior, Tab exit, Escape, outside dismissal, and focus return.",
    "Verify command, checkbox, radio, group, indicator, separator, and submenu behavior; typeahead; close policies; modal and parent-modal ownership; LTR and RTL; portals; and collision positioning."
  ],
  "related": ["menu", "context-menu", "menubar", "select", "nav-list"]
}
