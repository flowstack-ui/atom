{
  "schema": "flowstack.agent-component.v1",
  "id": "form",
  "name": "Form",
  "package": "@flowstack-ui/atom",
  "layer": "atom",
  "kind": "component",
  "purpose": "Provide the native form submission boundary and shared validation behavior for descendant fields and groups.",
  "useWhen": ["A set of controls submits related user input."],
  "avoidWhen": [{ "condition": "Controls are unrelated or trigger immediate independent actions.", "useInstead": "Individual Field and Button compositions" }],
  "composition": ["Compose Form around Fieldset for related groups and Field around each single control; finish with a named submit Button."],
  "rules": [
    { "id": "form-native-submit", "level": "must", "statement": "Preserve native form submission and do not make click handling the only submit path." },
    { "id": "form-control-names", "level": "must", "statement": "Give submitted controls stable names." }
  ],
  "commonMistakes": [{ "mistake": "Building a form-shaped div with click-only submission.", "correction": "Use Form and a submit Button so keyboard and platform behavior remain available." }],
  "validation": ["Submit with Enter where appropriate.", "Inspect submitted names and values.", "Verify invalid fields expose their relationships."],
  "related": ["field", "fieldset", "button"]
}
