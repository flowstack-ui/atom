{
  "schema": "flowstack.agent-component.v1", "id": "sidebar", "name": "Sidebar", "package": "@flowstack-ui/atom", "layer": "atom", "kind": "component",
  "purpose": "Coordinate expanded, rail, and offcanvas sidebar state across a trigger, complementary panel, and associated main region.",
  "useWhen": ["An application shell has a persistent side region that can collapse to a rail or leave the layout offcanvas."],
  "avoidWhen": [{ "condition": "The side panel is a temporary modal overlay or ordinary static aside.", "useInstead": "Drawer or a native aside" }],
  "composition": ["Compose Trigger, Panel, and Main inside Root; place persistent navigation such as NavList inside Panel."],
  "rules": [{ "id": "sidebar-one-main", "level": "must", "statement": "Use Sidebar.Main only when it is the page's single main landmark, or custom-render it without creating duplicate main landmarks." }, { "id": "sidebar-offcanvas-inert", "level": "must", "statement": "Keep offcanvas Panel content inert and outside the accessibility tree through the primitive contract." }],
  "commonMistakes": [{ "mistake": "Using Sidebar as a modal drawer or placing another main landmark inside Sidebar.Main.", "correction": "Use Drawer for modal panels and preserve one main landmark." }],
  "validation": ["Test controlled and uncontrolled expanded, rail, and offcanvas states and trigger announcements.", "Confirm offcanvas controls cannot receive focus and landmark names are unique."],
  "related": ["nav-list", "drawer", "app-bar"]
}
