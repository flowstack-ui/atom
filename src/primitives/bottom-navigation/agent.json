{
  "schema": "flowstack.agent-component.v1", "id": "bottom-navigation", "name": "BottomNavigation", "package": "@flowstack-ui/atom", "layer": "atom", "kind": "component",
  "purpose": "Provide a named navigation landmark with active state for a short, stable set of primary application destinations.",
  "useWhen": ["A compact application has a small stable set of top-level destinations commonly presented at the bottom."],
  "avoidWhen": [{ "condition": "The items are long, grouped, or switch panels within one page.", "useInstead": "NavList or Tabs" }],
  "composition": ["Compose Item parts with unique values inside Root; provide href for real destinations and omit it only for application-controlled view changes."],
  "rules": [{ "id": "bottom-nav-destination-semantics", "level": "must", "statement": "Use link Items for URL destinations and button Items only for view changes." }, { "id": "bottom-nav-label", "level": "must", "statement": "Keep authored label text available as each Item's accessible name even when the styled layer hides it." }],
  "commonMistakes": [{ "mistake": "Using BottomNavigation for a large menu or hiding labels from assistive technology.", "correction": "Use NavList for larger navigation and preserve every Item name." }],
  "validation": ["Confirm the landmark name distinguishes it from other navigation.", "Confirm the active destination exposes aria-current and disabled Items cannot navigate."],
  "related": ["nav-list", "tabs", "link"]
}
