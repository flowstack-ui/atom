# ScrollArea Changelog

## Unreleased

- Name the engine context for React DevTools diagnostics.

- Added measured custom scrollbar anatomy and controller commands while preserving
  native viewport input and the orientation-only context API.
- Added RTL progress, physical edge state, reduced-motion commands, pointer-capture
  dragging and dynamic overflow observation.

- Added public Agent Knowledge for component selection, required composition,
  recurring mistakes, and validation.

## 0.1.0

- Initial Atom release.
