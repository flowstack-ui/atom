# Breadcrumb Changelog

## Unreleased

- Expanded the README with hierarchy-selection guidance, corrected Anatomy and
  API order, complete ARIA contracts, and runnable examples.
- Changed `Breadcrumb.Root` to use the Atom-style `ariaLabel` prop for the
  breadcrumb navigation landmark label while still rendering native
  `aria-label`.

## 0.1.0

- Initial Atom release.
