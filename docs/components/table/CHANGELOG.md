# Table Changelog

## Unreleased

- Split every public Table part into its own API section and added usage,
  native accessibility, and runnable sorting guidance.
- Updated `Table.Head` so the default `scope` prop is only applied on the native `th` render path.

## 0.1.0

- Initial Atom release.
