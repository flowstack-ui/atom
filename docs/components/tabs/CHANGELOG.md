# Tabs Changelog

## Unreleased

- Expanded Tabs documentation with usage guidance, complete part-level ARIA
  and data contracts, context exports, and runnable examples.
- Added `dir` and `Direction.Provider` support so horizontal tab arrow-key
  navigation mirrors in RTL.
- Corrected the public API docs to describe `Tabs.Content keepMounted` and the
  measured `Tabs.Indicator` contract.

## 0.1.0

- Initial Atom release.
