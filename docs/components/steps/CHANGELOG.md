# Steps Changelog

## Unreleased

- Add useSteps, RootProvider, coordinated IDs, progress percentage and richer item state.
- Validate forward navigation outside linear mode and support optional-stage skipping.
- Recover focus from both hidden and unmounted owned panels without claiming nested workflows.

- Add ordered workflow state, guarded navigation, optional triggers, panels,
  completion content, contexts and public steps entrypoint.
