# List Changelog

## 0.22.6 - 2026-08-10

- Added public Agent Knowledge for native list semantics, ordered meaning,
  non-interactive ownership, disabled metadata, and server-safe composition.

## 0.9.1

- Fixed `List.Root` declarations to accept the documented native ordered-list
  `start` and `reversed` attributes when `ordered` renders an `ol`.
## 0.1.0

- Initial Atom release.
